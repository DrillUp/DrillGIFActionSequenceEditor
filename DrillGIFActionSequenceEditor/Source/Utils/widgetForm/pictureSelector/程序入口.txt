	【注意，对象选择器的全部以 code_piece_qt笔记 为准，所有修改在笔记中强化】
			
	程序入口（独立控件块）：
		p_PictureSelector.cpp
		p_PictureSelector.h
		注意，该 列表控件 被装饰后，最好对该控件块进行交互，不要与 列表控件 进行直接交互。
		该选择块，只有【单次读取载入】的结构，如果你还要【添加、删除、换位置】的功能，去看看继承的子类： 动画帧编辑块。 

	交互对象：
		c_PiSConfig.cpp
		c_PiSConfig.h
		图片选择块的配置。

	私有类：
		i_PiSPicture.cpp
		i_PiSPicture.h
		该类定义图片块中绘制图片的 QWidget 块。

		p_PiSBlock.cpp
		p_PiSBlock.h
		p_PiSBlock.ui
		该类定义每个 图片块 含有的哪些部件，注意，这些部件的坐标位置，仍然需要手动调整。
		
	诡异问题：
		【iconMode下出问题】，listMode没问题。
		直接additem，滚动条会被理所当然地撑开，但是使用op_append之后（后期additem），滚动条总会 漏 一个item。
		初步确认，不是op_append的问题，改变config，rebuild重新addItem之后，一样会出现此问题。