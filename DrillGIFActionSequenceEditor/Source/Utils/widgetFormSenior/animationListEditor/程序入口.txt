	【注意，对象选择器的全部以 code_piece_qt笔记 为准，所有修改在笔记中强化】
			
	程序入口（独立控件块）：
		p_AnimationListEditor.cpp
		p_AnimationListEditor.h
		注意，该 列表控件 被装饰后，最好对该控件块进行交互，不要与 列表控件 进行直接交互。
		该控件的【图标路径】见变量：this->m_iconSrcPath

		继承于p_PictureSelector图片选择块。
		部分继承的方法会被 私有化 ，需要调用新函数来代替。
		这里含一个特殊的信号：allFrameDeleted，全部帧被删除情况
		快捷键函数需要外部父类进行调用。
		
	可拆卸脚本：
		1. GIF的生成与拆解：
			s_GIFManager.h
			s_cximageManager.h
		2. 序列大图的生成与拆解：
			w_PictureCombiner.h
			w_PicturePartitioner.h
		上述的两个组件可以选择不添加（要修改脚本），也可以选择都添加（但是会关联一堆外部库）。

	交互对象：
		1.	c_ALEConfig.cpp
			c_ALEConfig.h
			动画帧的配置。继承于c_PiSConfig。

		2.	c_ALEData.cpp
			c_ALEData.h
			数据交互类，注意，获取后list中的元素顺序不一样也算作已修改。

	基于：
		p_PictureSelector.cpp
		p_PictureSelector.h
		s_GIFManager.cpp
		s_GIFManager.h
	
	私有类：
		p_ALEBlock.cpp
		p_ALEBlock.h
		继承于p_PiSBlock，在该控件块上，扩展一些控件。

		w_ALEDataEdit.cpp
		w_ALEDataEdit.h
		w_ALEDataEdit.ui
		编辑动画帧的单帧、多帧配置，耦合度高。不要把窗口拿出来用。

		w_ALEConfigEdit.cpp
		w_ALEConfigEdit.h
		w_ALEConfigEdit.ui
		编辑动画帧的整体ui设置，相对独立。窗口可以拿出来用，也可以直接调用openWindow_setConfigParam函数。

	大坑：
		1. QListWidget就是个垃圾控件。
		2. 【设置item的QWidget不能撑开控件。】通过图标 横向 挤压，字符\n 纵向 挤压，才能勉强将控件完全撑开。
		3. 【insertItem没用】，还好数据和ui是分离的，设置addItem，然后刷新item，就能实现撑开添加。
		
	诡异问题：
		【iconMode下出问题】，listMode没问题。
		直接additem，滚动条会被理所当然地撑开，但是使用op_append之后（后期additem），滚动条总会 漏 一个item。
		初步确认，不是op_append的问题，改变config，rebuild重新addItem之后，一样会出现此问题。